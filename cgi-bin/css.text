<style>
/* Importar tipografías industriales modernas */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=JetBrains+Mono:wght@400;700&display=swap');

/* Reglas Globales */
body { 
    font-family: 'Inter', sans-serif; 
    margin: 30px; 
    background-color: #0f172a; /* Slate 950 */
    color: #cbd5e1; /* Slate 300 */
}

/* Scrollbars Industriales */
::-webkit-scrollbar { width: 8px; height: 8px; }
::-webkit-scrollbar-track { background: #0f172a; }
::-webkit-scrollbar-thumb { background: #334155; border-radius: 4px; }
::-webkit-scrollbar-thumb:hover { background: #475569; }

/* Títulos y Secciones */
h2 { 
    color: #06b6d4; /* Cian Neón */
    background-color: #1e293b; /* Slate 800 */
    padding: 12px 18px; 
    border-radius: 4px;
    border: 1px solid #334155;
    text-transform: uppercase;
    font-size: 14px;
    letter-spacing: 0.1em;
    margin-top: 40px;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

h3 { 
    color: #94a3b8; 
    background-color: transparent; 
    padding: 5px 0; 
    border-bottom: 1px solid #334155; 
    font-size: 13px; 
    text-transform: uppercase;
}

/* Salidas de Comandos, CLI y Datos Raw */
pre {
    font-family: 'JetBrains Mono', monospace;
    background-color: #1e293b; 
    color: #10b981; /* Verde Esmeralda (Terminal) */
    padding: 18px;
    border: 1px solid #334155;
    border-radius: 4px;
    overflow-x: auto;
    font-size: 13px;
    box-shadow: inset 0 2px 4px rgba(0,0,0,0.5);
    line-height: 1.5;
}

/* Tablas de Red */
table { 
    border-collapse: collapse; 
    margin-bottom: 20px; 
    width: 100%; 
    background-color: #1e293b;
    border: 1px solid #334155;
    border-radius: 4px;
    overflow: hidden;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

td, th { 
    border-bottom: 1px solid #334155; 
    padding: 12px 15px; 
    text-align: left; 
    font-size: 13px;
}

th { 
    background-color: #0f172a; 
    color: #94a3b8;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-weight: 600;
}

tr:hover td {
    background-color: rgba(6, 182, 212, 0.05); /* Cian traslúcido */
}

/* Formularios e Inputs */
input[type="text"], input[type="password"], input[type="number"], select {
    background-color: #0f172a;
    border: 1px solid #334155;
    color: #10b981; /* Color emerald para el texto introducido */
    padding: 8px 12px;
    border-radius: 4px;
    font-family: 'JetBrains Mono', monospace;
    outline: none;
    transition: border-color 0.2s ease;
}

input[type="text"]:focus, select:focus {
    border-color: #06b6d4;
    box-shadow: 0 0 5px rgba(6, 182, 212, 0.3);
}

/* Botones (Submit, Acción) */
button, input[type="submit"] { 
    padding: 8px 16px; 
    margin-left: 5px; 
    background-color: #06b6d4; /* Cian primario */
    color: #0f172a; /* Texto oscuro para contraste */
    border: none; 
    border-radius: 4px;
    cursor: pointer; 
    font-family: 'Inter', sans-serif;
    font-weight: 600;
    text-transform: uppercase;
    font-size: 12px;
    letter-spacing: 0.05em;
    transition: all 0.2s ease;
}

button:hover, input[type="submit"]:hover { 
    background-color: #22d3ee; 
    box-shadow: 0 0 12px rgba(6, 182, 212, 0.5); /* Resplandor neón */
    transform: translateY(-1px);
}

button:active, input[type="submit"]:active {
    transform: translateY(1px);
}

/* Enlaces (A) */
a { 
    color: #06b6d4; 
    text-decoration: none; 
    font-weight: 600; 
    padding: 4px 8px; 
    border-radius: 4px; 
    transition: all 0.2s ease; 
}

a:hover { 
    color: #0f172a; 
    background-color: #06b6d4; 
}

a:visited { color: #06b6d4; }
a:visited:hover { color: #0f172a; }

</style>
